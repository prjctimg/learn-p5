Also instead of using a @scripts/hooks/ approach use Husky instead and keep the scripts directory clean for actual scripts we need to run during push and/or building.

Also delete the remote releases using the gh api

After that proceed with the changes. 
Called the Read tool with the following input: {"filePath":"/workspaces/learn-p5/scripts/hooks/"}
<path>/workspaces/learn-p5/scripts/hooks/</path>
<type>directory</type>
<entries>
commit-msg
post-commit
post-rewrite
pre-push
pre-push.pre-entire
prepare-commit-msg

(6 entries)
</entries>