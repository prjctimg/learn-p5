## Improvements- Make the popup modal vertically scrollable when the content is too long. Currently its not scrollable - Move the GitHub  activity component to be the last component on the dashboard- Remove the achievements section and instead, show the acquired achievements as overlapping circles, GitHub style below the progress bar (remove the heading that says "Achievements"- Make the codeblock scrollable, such that when the keyboard is active, no code is covered it. When the keyboard is toggled off, let the codeblock show 20 LOC maximum- Move the symbol keyboard toggle icon to the toolbar   (currently it is at the bottom row of the QWERTY keyboard) and make the rest of the symbols scroll beneath it